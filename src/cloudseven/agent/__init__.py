"""
Agent orchestration layer.

Phase 1: simple chat loop in `chatbot.py`.
Phase 3+: this gets replaced/extended with LangGraph (state.py, nodes.py, graph.py).

The interface stays the same: a Conversation object that you `.send(user_msg)` to.
"""
